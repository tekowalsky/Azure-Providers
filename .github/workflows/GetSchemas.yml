name: Update Schema List
on:
  workflow_dispatch:

jobs:
  checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2.4.0
        with:
          repository: Azure/azure-resource-manager-schemas
          path: ./azure-resource-manager-schemas
          fetch-depth: 0
          
      - name: SetupSchemaDir
        run: |
          mkdir ./schemas
          cp -rf ./azure-resource-manager-schemas/schemas/20* ./schemas
          rm -rf ./schemas/*-preview
          rm -rf ./schemas/*-privatepreview

      - name: Checkout My Repo
        uses: actions/checkout@v2.4.0
        with:
          repository: tekowalsky/Azure-Providers
          path: ./myrepo
          
      - name: MakeMerged
        run: |
          if [ -d ./myrepo/merged ]; then
            echo "Found ./myrepo/merged"
          else
            mkdir ./myrepo/merged
          fi
          for dir in ./schemas/* ; do
            for fn in $dir/*.json ; do
              cp -u $fn ./myrepo/merged
            done
          done

      - name: CommitAndPush
        run: |
          cd ./myrepo
          date > generated.txt
          git config user.name GetSchemas-Workflow
          git config user.email github-actions@github.com
          git add ./merged
          git commit -m "generated"
          git push
